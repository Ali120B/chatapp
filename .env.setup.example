# Copy to .env.setup and fill in — NEVER commit .env.setup or share API keys in chat
APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
APPWRITE_PROJECT_ID=paste_project_id_here
APPWRITE_API_KEY=paste_secret_api_key_here

# Optional — defaults shown
VITE_APPWRITE_DATABASE_ID=chatapp
APPWRITE_WEB_HOSTNAME=localhost
